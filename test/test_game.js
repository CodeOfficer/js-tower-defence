(function($) {
  // $(function() {
  with(QUnit) {
    context('Sammy','apps')
      .should('return a new application if no arguments passed', function() {
        var app = new Game();
        // defined(app, 'route');
				ok(true);
      })
      
     
    }
  // });
})(jQuery);
